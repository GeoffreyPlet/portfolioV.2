
 {% block stylesheets %}
    <link rel="stylesheet" href=" {{ asset('css/create-header.css') }}">
{% endblock %}
 
 
 <!-- #START [Form method = post ] -->



    <!-- #START filter-body -->
    <div class="p-5">

                    <!-- #START form -->
                    {{ form_start(headerForm) }}
                    

                        <div class="container">
                            <div class="row">
                                <div class="">

                                </div>
                            </div>
                            <div class="row justify-content-end" >
                                <div class="w-100">
                                    {{ form_label(headerForm.title, 'Titre') }}
                                    {{ form_widget(headerForm.title) }}
                                    {{ form_widget(headerForm) }}
                                </div>
                                <div>
                                    <input value="upload-{{header.id}}" name="action" hidden>
                                </div>
                                <button formnovalidate>Modifier</button>
                            </div>
                        </div>
                    {{ form_end(headerForm) }}
                    <!-- #END form -->

        </div>
        <!-- #END filter-body -->

       